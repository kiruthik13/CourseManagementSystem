"""
Instructors app — models module.

All instructor-related models live in the accounts app
(accounts.User with role=instructor and accounts.InstructorProfile).

This module is intentionally empty to keep the codebase clean and avoid
model duplication (constraint #38).
"""
# No models defined here. See accounts.models.InstructorProfile.
